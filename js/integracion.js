/**
 * Integración Unidad 3 (módulos ES del proyecto fuente) + pestañas U1/U3
 */
import { AppController } from './controllers/appController.js';

document.addEventListener('DOMContentLoaded', () => {
    AppController.init();
});
