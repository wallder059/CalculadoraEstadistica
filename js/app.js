/**
 * app.js - Punto de Entrada
 * Tutor de Estadística Descriptiva (Arquitectura MVC)
 */

import { AppController } from './controllers/appController.js';

document.addEventListener('DOMContentLoaded', () => {
    AppController.init();
});
